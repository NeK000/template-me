{
  "name": "hugo-template-linting",
  "version": "1.0.0",
  "description": "Linting and formatting for Hugo static site projects.",
  "scripts": {
    "lint:css": "stylelint 'static/css/**/*.css'",
    "lint:html": "htmlhint 'layouts/**/*.html'",
    "lint": "npm run lint:css && npm run lint:html"
  },
  "devDependencies": {
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0",
    "htmlhint": "^1.1.5"
  }
}
